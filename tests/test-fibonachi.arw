& Fibonacho[# n] X #
<
    ? n << 100
    <
        ? n << 1
        <
            n -> !
        >
        ? n <> 1
        <
            n -> !
        >
        # callone <- n - 1!
        # calltwo <- n - 2!
        # returnone <- Fibonacho[callone]!
        # returntwo <- Fibonacho[calltwo]!
        # return <- returnone + returntwo!
        return -> !
    >
    n -> !
>

& PrintFibonachi[# count, # index] X #
<
    # return <- Fibonacho[index]!
    ? index << count
    <   
        StringOut[{Fibon achi:}]!
        IntOutLine[return]!
        index <- index + 1!
        return <- PrintFibonachi[count, index]!
        return -> !
    >
    return -> !
>


& Main [] X #
<
    # output <- PrintFibonachi[10, 0]!
    output -> !
>